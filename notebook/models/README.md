# Trained Model

The trained machine learning model is normally saved here as:

- trained_model.pkl

The model is generated after training using Scikit-learn and can be loaded later for predictions.
# Machine Learning Workflow

This notebook includes the complete machine learning pipeline.

## Steps Performed

1. Loaded Iris dataset
2. Data preprocessing
3. Exploratory Data Analysis (EDA)
4. Train-Test Split
5. Logistic Regression model
6. Random Forest model
7. Model Evaluation
8. Accuracy, Precision, Recall and F1-Score comparison
9. Business insights

Libraries Used:
- pandas
- numpy
- matplotlib
- seaborn
- scikit-learn
